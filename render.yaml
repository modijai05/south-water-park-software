# 🔧 RENDER DEPLOYMENT CONFIGURATION - COMPLETE SYSTEM

services:
  # Backend Service Configuration
  - type: web
    name: south-water-park-backend
    env: node
    plan: free
    buildCommand: "cd backend/server && npm install"
    startCommand: "cd backend/server && npm start"
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: MONGODB_URI
        value: mongodb+srv://jaimodi05bapa_db_user:SgKNnsz19WTuvHp3@cluster.nckewmo.mongodb.net/south_water_park?retryWrites=true&w=majority
      - key: JWT_SECRET
        value: south-water-park-production-secret-2024-secure
      - key: CLIENT_URL
        value: https://endearing-kleicha-b78d8e.netlify.app
      - key: TZ
        value: UTC
    domains:
      - south-water-park-backend.onrender.com

  # Frontend Service Configuration (Optional - can use Netlify)
  - type: web
    name: south-water-park-frontend
    env: static
    plan: free
    buildCommand: "cd frontend/client && npm install && npm run build"
    staticPublishPath: frontend/client/dist
    envVars:
      - key: VITE_API_URL
        value: https://south-water-park-backend.onrender.com/api
    domains:
      - south-water-park-frontend.onrender.com
